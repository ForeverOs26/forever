# Changelog

## v0.1.0

- FDB-001 completed.
- FDB-002 completed for Modeva import foundation and validation.
- FDB-003A completed: Forever Import Engine v1 created.
- FDB-003B completed: Modeva dry-run passed.
- FDB-003C completed: Modeva real import/idempotency test passed.
- RC3-001 completed: Import Engine architecture skeleton added with explicit import plans, relationship validation, rollback contract, and state machine.
- RC3-002 completed: first Project-only import stage added with Coralina dry-run blocked safely by readiness validation.
- RC3-003 completed: Buildings-only Import Engine stage added after Project for source-backed dry-run planning.
- RC3-004 completed: Canonical Unit Import dry-run stage added after Buildings.
- RC3-005 completed: Price History dry-run import stage added after Units.
- FDM-001 completed.
- FDM-002 completed.
- RC2.5 completed: Advisory Project Summary — a concise, evidence-only executive summary that summarises the verified project data and the already-derived Forever Passport and Intelligence outputs, with no new scoring engine.
- RC2.6 completed: Advisory Project Comparison — the first comparison engine, a descriptive, evidence-only comparison of two projects built on top of the already-derived Forever Passport and Project Summary, with no new scoring engine, ranking, or fabricated values.
- RC2.7 completed: Advisory Project Recommendations — an evidence-only recommendation layer that ranks the available projects using the already-derived Forever Passport, Project Summary and Project Comparison. Ordering is a deterministic sort over documented readiness stage, present verified evidence-signal counts, and recorded data-gap counts only; it introduces no new scoring engine or match score, duplicates no derivation logic, fabricates no values, uses no marketing language, and renders missing data as "Not available".
- RC2.8 completed: Advisory Advisor Report — a professional, client-facing, print-ready advisory report that composes the already-derived Forever Passport (RC2.4), Project Summary (RC2.5), Investment / Rental / Location Intelligence (RC2.1–RC2.3) and, when present, Project Comparison (RC2.6) and Project Recommendations (RC2.7) into one coherent document. It is a pure, deterministic presentation-and-composition layer: it adds no new score, verdict, ranking, persona or financial metric, never recalculates existing conclusions, never exposes the hidden numeric trustScore, and renders missing data as "Not available". Optional sections stay absent when their data is unavailable, the report date appears only when explicitly supplied, and printing uses the browser's own print flow (Print / Save as PDF) with no heavy PDF dependency. Delivered as an isolated `/advisory/report` route; the existing Advisory Workspace is unchanged.
- RC4.4 completed: Forever Source Registry Foundation (`forever-project-sources`) — the canonical, architecture-only catalogue of every source document that enters the Forever ecosystem (price lists, brochures, floor/master/unit plans, contracts, legal documents, marketing materials, specifications, developer updates), with a deterministic in-memory registry, deterministic id derivation, and a validation pipeline that never throws. 14 test files, 69 tests; no existing file changed.
- RC4.5 completed: Forever Extraction Pipeline Foundation (`forever-extraction-pipeline`) — the architecture-only description of how a registered RC4.4 source produces structured extracted facts: extraction plans, facts with confidence/evidence/provenance, and conflicting readings that coexist rather than resolve. Deterministic, pure, no OCR/AI/IO. 19 test files, 113 tests; no existing file changed.
- RC4.6 completed: Forever Canonical Project Database Foundation (`forever-project-database`) — the canonical destination of the intake chain: versioned canonical fields with append-only value history, append-only revisions, frozen snapshots, an audit timeline, and a pure `describeProjectMerge` that never resolves a cross-source conflict. Architecture only, no persistence. 21 test files, 161 tests; no existing file changed.
- RC4.7 completed: Forever Cross-Source Validation Foundation (`forever-cross-validation`) — deterministically examines a batch of RC4.5 extracted facts against their RC4.4 registered sources and describes agreement, conflict, and admissibility toward the RC4.6 canonical database, without ever resolving a disagreement itself. 15 test files, 133 tests; no existing file changed.
- RC4.8 completed: Forever Project Knowledge Graph Foundation (`forever-knowledge-graph`) — describes the knowledge graph a project's registered sources, extracted facts, canonical record, and cross-source validation findings add up to, with uncertainty preserved and every node and edge traceable to the artifact or declaration that states it. 16 test files, 143 tests; no existing file changed.
- RC4.9 completed: Forever Project Readiness Foundation (`forever-project-readiness`) — the exit gate of the intake chain: deterministically judges, but never approves, whether a project's accumulated knowledge satisfies caller-stated requirements against the canonical record, cross-source validation report, and registered sources, formalizing the readiness audits the repository previously kept by hand. 16 test files, 131 tests; no existing file changed.
- RC5.0 completed: Coralina End-to-End Vertical Slice (`coralina-knowledge`) — real, committed Coralina source data (brochure, price list V.2, facilities, location map, unit plans, master plan) run through the complete RC4.4–RC4.9 foundation chain, exposed at the internal-only route `/internal/coralina` (`noindex`, not linked, dynamically imported). Readiness is honestly `blocked` on the same two real Coralina manifest gaps, `developer` and `country`. 61 integration/regression tests covering the full chain, traceability, honesty, and determinism.
- RC5.1 completed: Project Knowledge Platform — the RC5.0 Coralina vertical slice generalised into a project-agnostic engine (`forever-project-knowledge`): a declarative `ProjectKnowledgeDefinition` (sources, verbatim facts, declared gaps, graph declarations, readiness profile — statements only) run through the RC4.4–RC4.9 foundation chain by one shared orchestration. Coralina re-stated as a definition with all 61 RC5.0 tests passing unchanged; Modeva onboarded as the second real project purely from committed artifacts (FDB-001 seed migration, FDB-002C reviewed price-list import, FDB-003C real-run report) with an honestly BLOCKED intake verdict (no committed brochure); one generic internal inspection route `/internal/projects/$slug` serving every catalogued project. See `docs/RC5_1_PROJECT_KNOWLEDGE_PLATFORM.md`.
- RC5.2 completed: Architecture State and Roadmap Alignment — documentation and governance only. Reconciled `docs/CURRENT_STAGE.md`, `docs/FOREVER_STATUS.md`, `docs/ROADMAP.md`, `docs/FOREVER_BLUEPRINT.md`, and `docs/FOREVER_BRAIN_V1.md` with the completed RC4.4–RC5.1 chain; added the missing RC4.4–RC5.0 changelog entries above; recorded the decision in `docs/DECISIONS.md`. No application code, database, or route changed.
- RC5.3 completed: Coralina Source-Backed Intake Completion — re-audited the two declared Coralina blockers (`developer`, `country`) against every committed source artifact (manifest, import status, classification log, all six extracted JSON datasets, every `source/*` folder) and found no new evidence since RC5.0, so no fact was added for either. Extended the two gap reasons in `src/features/coralina-knowledge/facts.ts` with the exact source-acquisition requirement for each field (data only); readiness standing is unchanged (`blocked`); the unit-type dispute stays unresolved. New regression tests pin the evidence audit to the committed repository state and prove both blockers stay unresolved. See `docs/CORALINA_RC5_3_EVIDENCE_AUDIT.md`.

- RC5.4 completed: Coralina Official-Source Intake Validation and Dry-Run Completion — official corporate and Thailand SEC-hosted evidence resolved the `developer` and `country` blockers, readiness became `ready`, and the Import Engine produced a deterministic 405-operation dry-run (1 project, 8 buildings, 198 units, 198 price-history rows). The transparent Thailand-to-THB `inferred_default` currency decision remains auditable. Execute mode did not run and database writes remained zero.
- RC5.5A completed and merged: Target and Preflight Guards — deterministic canonical SHA-256 operation-plan fingerprints, typed non-persistent dry-run receipts, explicit local/staging/production target definitions, pure fail-closed preflight checks, and minimal CLI/importer integration. Production remains unconditionally blocked, staging remains unconfigured, valid local preflight still ends at the execute-disabled boundary, and no database connection, query, write, schema change, or dependency change was introduced.
- RC5.5B completed, merged, and locally proven: Read-Only Target Collision Inspection — an explicitly requested `--inspect-collisions` mode that, only after a successful RC5.5A local preflight, reads only the plan-referenced target rows required to compare the approved Import Plan with the existing project, buildings, units, and price history, then stops. Comparison uses shared persistence-projection helpers (`src/import/persistence-projection.ts`) extracted from `database.ts` so the inspector compares exactly the stable fields the write path would persist — including project foreign keys (`developer_id`, `location_id`) resolved through bounded developer/location dependency reads, deterministic JSONB metadata (order-independent), and `recorded_at` — excluding only genuinely volatile runtime values (`updated_at`, runtime `last_data_review_at`); the extraction changes no write behavior. Findings are classified `absent`, `exact_match`, `update_required`, `duplicate_target_rows`, `identity_conflict`, or `inspection_error`; a partial comparison can never be labeled `exact_match`. Every returned target row is strictly validated before comparison (id, parent identity, natural-key presence, field shapes); a malformed row becomes a blocking `inspection_error` and is never silently dropped to `absent`. The operation set is validated before any read: unsupported entities, not-exactly-one project, duplicate keys, payload/entity mismatch, and count mismatch fail closed with zero adapter calls, and every supported operation yields exactly one finding (`totalInspectedOperations === findings.length === operations.length === operationCounts.operations`). The write path's exact pre-RC5.5B semantics are preserved: the shared unit projection accepts `string | null | undefined` for `building_id` and passes it through verbatim, so an unresolved building code still omits `building_id` (undefined) rather than clearing it (null), and the inspector treats an unresolvable expected building identity as a blocking `identity_conflict` before any field comparison. Beyond the plan-key checks, the operation set validation confirms every operation's natural key equals its planner form (`slug`, `${slug}:${buildingCode}`, `${slug}:${unitNumber}`, and the full price-history key), every per-entity count invariant (`projects`/`buildings`/`units`/`priceHistoryRows`/`operations`), and that no two price-history operations collapse to the same database persistence key (which excludes `sourceRow`). Reads are plan-key-bounded and batched (project by slug; developer/location by slug; buildings/units by resolved `project_id` and planned code batches; price history by resolved unit ids); each logical query requests the exact PostgREST row count and paginates by ascending immutable `id` until the uniquely collected rows equal that count — completeness is proven by the count, never inferred from a short page, so a server-side maximum-row cap, a missing/malformed/shifting count, rows exceeding the count, duplicate ids across pages, or a non-advancing page all fail closed as `incomplete_result`/`pagination_inconsistent` rather than a false `absent`; a zero-row result is accepted only when the exact count proves zero. Malformed target price-history rows are never dropped: a malformed row whose persistence-key fields are establishable blocks the operation with that exact persistence identity (even when a valid row also matches, preventing a false `exact_match`), and a malformed row whose key cannot be established blocks every operation on its parent unit (`persistence_key_unresolvable`), so a malformed row can never decay into `absent`. Developer and location dependencies are strictly validated (exactly one row; non-empty id; non-empty slug equal to the requested slug). Malformed-row diagnostics and read failures are deterministic and sanitized: per-key error codes are deduplicated and sorted so the report is byte-equivalent regardless of target-row return order, and provider/network failures are mapped to stable safe codes (`project_read_failed`, `dependency_read_failed`, `building_read_failed`, `unit_read_failed`, `price_history_read_failed`, `incomplete_result`, `pagination_inconsistent`) so no URL, credential, or raw provider message can enter a report or the logger. The reader is a closure-based select-only adapter that never exposes the Supabase client as a runtime property and only ever issues `from`/`select`/`eq`/`in`/`order`/`range`; it uses `SUPABASE_PUBLISHABLE_KEY` exclusively, never reads or falls back to `SUPABASE_SERVICE_ROLE_KEY`, and fails closed before any client or network creation when the publishable key is missing. `--dry-run` stays client-, read-, and network-free; `--dry-run` combined with `--inspect-collisions` and inspection without target/hash/confirmation fail closed; production stays blocked; staging stays unconfigured; local still requires the approved `forever-local` identity. Every report states `readOnlyConfirmed: true`, `executeEnabled: false`, and `writesPerformed: 0`; a successful inspection never enters write execution. Hermetic tests (including a 405-operation Coralina-shaped fixture: 1 project, 8 buildings, 198 units, 198 price-history operations) cover full-field comparison, dependency resolution, strict validation, operation coverage, bounded/batched queries, the credential boundary, and the read-only proof without any gitignored source document. The Owner's local read-only proving run against the reconciled canonical local target reported Coralina as 405 `absent` operations (1 project, 8 buildings, 198 units, 198 price-history rows) with zero collisions, duplicates, identity conflicts, or inspection errors.

- RC5.5C completed, reviewed, integrated, and merged: Transactional Execution and Rollback Preparation — a single-transaction execution boundary that applies one approved import plan atomically and never permits partial success. A narrow typed mutation interface (`execution-adapter.ts`) exposes only entity-specific inserts and bounded verification read-backs — no generic table access, raw SQL, generic RPC, or delete — and the write path reuses the shared RC5.5B persistence projections so the inspector and executor can never drift (undefined/null `building_id` semantics and persistence keys preserved, `sourceRow` excluded). Execution requires an explicit `--execute-approved-import` request plus target, fresh plan hash, confirmation, successful RC5.5A preflight, a fresh unblocked all-`absent` RC5.5B collision report, and a valid Owner approval artifact (`execution-approval.ts`) bound to project slug, target identity, plan hash, operation count, collision-report fingerprint, issue/expiry timestamps (max one-hour lifetime), and a one-time-use id; approvals fail closed when missing, malformed, expired, not yet valid, over-lifetime, reused, out of scope, or schema-unsupported; the raw approval id is used internally for atomic consumption only and never appears in a receipt or log — every external surface carries only a deterministic domain-separated SHA-256 digest (`forever-import-approval:v1`), or null when no format-safe id existed; single use is enforced atomically at the execution-attempt boundary through the asynchronous, durable-ready `consumeIfUnused` compare-and-set so exactly one confirmed CAS winner of any number of concurrent attempts can proceed, earlier-rejected requests never consume, a rolled-back winner remains consumed, and a registry infrastructure failure is contained (`approval_registry_unavailable`: runner never invoked, raw error discarded, approval truthfully unconsumed) — the in-memory registry is hermetic test infrastructure only and any future live implementation requires a durable atomic backing store. Operations apply in canonical order (dependencies read first, then project → buildings → units → price history) after the operation set, ordering, parent references, and per-entity counts validate with zero adapter calls otherwise; before commit, every persisted row is verified inside the same transaction against the shared projections (counts, fields, parents, persistence-key uniqueness), and any failure category — adapter error, dependency failure, verification mismatch, state drift after inspection (`target_state_changed`), or commit failure — rolls back automatically with deterministic reason codes drawn from a closed stable-code whitelist — untrusted runner codes, provider messages, credentials, URLs, SQL text, and raw rows all collapse to `adapter_failure` and can never reach a receipt or the logger. Runner-level throws are contained and never misclassified: any runner-level throw or malformed runner outcome — before or after the work callback, since a runner may begin a transaction before invoking work — yields the truthful `failed_rollback_unconfirmed` outcome with `writesPerformed: null` (exact applied count for `committed`, 0 for `rolled_back` and `rejected_before_transaction`; the receipt never claims zero writes when the transaction outcome is unknown, and the logger prints the unknown state as `unconfirmed`), `rejected_before_transaction` is reserved exclusively for executor gates that fail before the runner is invoked, and `rollbackConfirmed` is never falsely set. The typed execution receipt reports outcome (`committed`/`rolled_back`/`rejected_before_transaction`/`failed_rollback_unconfirmed`), attempted/applied counts, commit/rollback confirmation, approval consumption, `executeEnabled: false`, and `writesPerformed`; a committed receipt is impossible unless every operation and the verification and the commit all succeeded. Repeat execution fails closed: any non-`absent` target state is rejected before the transaction. The live transaction runner is structurally defined but disabled (`live_execution_disabled`) before any credential read or client creation; hermetic tests (including the 405-operation Coralina-shaped fixture committing and rolling back with zero partial writes) use fake transaction and approval adapters only. No real import has occurred, real database writes remain zero, production and staging remain blocked, Factory autonomy remains A0, and the first real Coralina import remains a separate Owner checkpoint.
- RC5.5D implemented (server-side live-execution-boundary preparation only, pending Owner review and integration): Live Execution Boundary Preparation — prepares, without activating, everything the future first real Coralina import needs to run as ONE server-side PostgreSQL transaction instead of multiple independent Supabase REST insert calls. A committed-but-NOT-applied migration (`supabase/migrations/20260715120000_rc55d_import_execution_boundary.sql`) defines a private `forever_import` schema (PUBLIC/anon/authenticated fully revoked, RLS enabled, not PostgREST-exposed) with durable approval storage (domain-separated SHA-256 digest only — never a raw approval id or token; UNIQUE digest; CHECK-enforced formats, operation ceiling, and max one-hour lifetime; `consumed_at`/`execution_id` pairing), durable execution receipts (UNIQUE `(project_slug, plan_hash)` permanently blocks re-importing an already-imported plan even with a fresh approval), and one bounded execution function reachable only through the SECURITY DEFINER wrapper `forever_execution.forever_execute_approved_import(jsonb)` in a SEPARATE dedicated closed schema (NOT `public`; PUBLIC revoked, default function EXECUTE for PUBLIC reversed; pinned empty `search_path`; EXECUTE revoked from PUBLIC/anon/authenticated AND `service_role`, granted only to the dedicated least-privilege LOGIN role `forever_import_executor`; the approval-issuance function is granted to NO role; no dynamic SQL anywhere). Inside that single transaction the server validates the versioned bounded request (exact key sets, formats, counts, 1000-operation and 4 MiB ceilings, duplicate natural/persistence keys), atomically consumes the durable approval (row-lock CAS; expiry evaluated on DATABASE time; exactly one concurrent winner), re-verifies fresh target state independently of the client-side collision report, inserts project → buildings → units → price history in canonical order, verifies every persisted row, count, relationship, and persistence-key uniqueness before commit, writes the receipt, and returns one typed sanitized result — any mismatch raises and rolls back everything including the approval consumption, so a rolled-back import never leaves a falsely consumed durable approval and partial import is impossible. Client-side, a typed versioned server request contract (`server-execution-request.ts`) reuses the shared RC5.5B/RC5.5C projections and validators verbatim (no second persistence-mapping implementation) with a deterministic canonical form and domain-separated fingerprint, rejecting unknown properties, duplicate keys, unresolved parents, raw local paths, credential material, and any fingerprint tamper; an isolated execution credential boundary (`execution-credentials.ts`) reads ONLY the dedicated `FOREVER_IMPORT_EXECUTOR_DATABASE_URL` (never `SUPABASE_SERVICE_ROLE_KEY`), lazily and only on the live path, and strictly parses it into one of two approved routes for the dedicated executor role — the direct `db.<ref>.supabase.co:5432` endpoint or the IPv4-capable Supavisor SESSION pooler `aws-<n>-<region>.pooler.supabase.com:5432` (deriving role and project ref independently from the `<role>.<ref>` pooler username, rejecting the transaction-mode port 6543, arbitrary/deceptive pooler hosts, Supabase API keys, HTTPS URLs, foreign roles, and deviant host/port/path forms), so the IPv4-only Owner environment that reproduced `LegacyDbConfigIpv6Error` has an approved route with no paid IPv4 add-on; the boundary structurally cannot leak the connection string or password through JSON, enumeration, spread, or inspection; and a typed live adapter (`live-execution-adapter.ts`) performs exactly one fixed direct-PostgreSQL statement (`SELECT forever_execution.forever_execute_approved_import($1::jsonb)`, not a PostgREST RPC) with no retry, no client-side transaction simulation, and no generic Supabase client escape — invalid requests are rejected before the credential provider or any transport exists, raw provider errors are discarded, only exact whitelisted server reason codes survive (a whitelisted code can only originate from the boundary function's controlled RAISE, which PostgreSQL guarantees rolled back), and timeouts, transport throws, and malformed/partial/mismatched responses map truthfully to `failed_rollback_unconfirmed` with `writesPerformed: null`. The server-boundary executor (`server-execution.ts`) reuses the exact RC5.5C gate pipeline (operation set, ordering, RC5.5A preflight, fresh all-absent RC5.5B collision gate, Owner approval validation) plus the local one-time registry CAS, and maps outcomes to the RC5.5C receipt vocabulary with a display-safe `executionId` on commit. The executed payload is cryptographically and structurally bound to the Owner approval (architect review blocker 1): approval registration receives the complete bounded approved request, validates it with the same shared server-side validator the execution path uses (malformed payloads, unsupported properties, payload/count mismatches, duplicate keys, oversized requests, unsafe paths, and credential material all fail closed at registration), stores it as an immutable canonical JSONB body whose digest is computed inside PostgreSQL, and CHECK-binds every scope column to that body; at execution the incoming request must be structurally identical to the stored approved request and match the PostgreSQL-recomputed digest BEFORE durable consumption and before any write, so a self-consistent malicious client that alters a project field, developer/location slug, status, building metadata, unit price/bedrooms/parent, price-history amount, source identity, entity count, array order, or the fingerprint itself is rejected server-side (`approval_request_mismatch`/`approval_scope_mismatch`) with zero consumption and zero writes — the client-supplied `requestFingerprint` is never trusted as proof of payload identity. Execution is endpoint-identity-bound (architect review blocker 2, `execution-endpoint.ts`): before transport creation and before any network access, `SUPABASE_URL` must strictly parse (HTTPS only; no userinfo, port, path, query, fragment, or whitespace/quote/control characters; exact `<ref>.supabase.co` form) and its ref must equal the canonical Forever project `abtvsrcnfwlbawvrjeed` and agree with both the committed approved-target configuration and the request's target identity; HTTP, arbitrary hosts, deceptive prefix/suffix hostnames, userinfo, query/fragment variants, wrong refs, and identity disagreements all fail closed with zero transport and zero network calls and without leaking any URL. Execution authenticates as a dedicated least-privilege principal (architect review blocker 3): `service_role` is removed from the wrapper grant entirely — it already holds broad direct table privileges, so granting it the wrapper would narrow nothing and let a service-role-key holder bypass the whole boundary — and the ONLY execution principal is `forever_import_executor`, a dedicated LOGIN role (design A, reached through a future direct database transport) whose two required grants are USAGE on the dedicated closed `forever_execution` schema plus EXECUTE on the SECURITY DEFINER wrapper, with every direct table, private-schema, and internal-function privilege — and direct `public`-schema grants — revoked as defensive hygiene (a direct REVOKE does not override a PUBLIC-derived grant), no service*role membership, NOINHERIT, no ambient authority, and no committed password (fail-closed until the Owner provisions a secret separately); because the wrapper is SECURITY DEFINER it performs the import as its owner while the executor holds no table privileges, so a caller possessing only the execution credential can invoke the one bounded wrapper but cannot directly read or mutate the imported tables or the approval/receipt storage; the credential is the dedicated `FOREVER_IMPORT_EXECUTOR_DATABASE_URL`, never the service-role key. Least privilege is proven at the EFFECTIVE level, not by direct-grant enumeration (architect review blocker 5): the external wrapper lives in a dedicated closed `forever_execution` schema (not `public`), the executor is granted USAGE there plus wrapper EXECUTE, and its direct `public`-schema grants are revoked only as defensive hygiene — a direct REVOKE does NOT override a PUBLIC-derived grant (every role is implicitly part of PUBLIC, and a stock database grants `public` USAGE to PUBLIC), so the executor's effective `public` access is proven only by an effective-privilege & PUBLIC-ACL audit module (`effective-privilege-audit.ts`) of read-only single-SELECT catalog checks that measure the executor's effective privileges via `has_schema_privilege`/`has_function_privilege`/`has_table_privilege`/`has_sequence_privilege`/`has_database_privilege` (role and object resolved to OIDs, fail-closed on absence) AND inspect the PUBLIC grantee (OID 0) through the ACL catalogs (`pg_namespace.nspacl`/`pg_proc.proacl` via `aclexplode` over `COALESCE(acl, acldefault(...))`, never passing `'public'` as a role to `has*\*\_privilege`), plus ownership (`pg*database`/`pg*namespace`/`pg\*class`/`pg_proc`) and sequence authority, evaluating the ENTIRE non-system database surface (including EXECUTE on ANY routine — functions, procedures, aggregates, window routines, SECURITY DEFINER or INVOKER — so the wrapper is the sole routine the executor may execute; explicit per-schema classification into approved_required/explicitly_prohibited/unexpected surfaces, where a newly introduced schema blocks readiness until the Owner classifies it; and complete execution-chain validation rejecting a SECURITY DEFINER wrapper owner or internal-routine owner that is a superuser/BYPASSRLS/broad platform role or a member of one, with the executor confirmed a dedicated no-ambient-authority LOGIN role; and (review 9) binding the whole chain to ONE EXACT dedicated NOLOGIN owner role `forever_import_execution_owner`— the migration creates it and reassigns the two boundary schemas, two durable tables, and six routines to it, and the audit proves the wrapper and every internal routine are owned by EXACTLY that role (never postgres/service_role/a platform role/the executor), that the owner has an empty direct-and-recursive membership/SET ROLE closure, that it owns nothing outside the approved boundary inventory (and no database), and that its effective capabilities reduce to the exact target allowlist (USAGE on public; SELECT on the two dependency tables; SELECT + INSERT on the four import tables; no UPDATE/DELETE/TRUNCATE, no sequence privilege since every target uses a UUID key); and (review 10) proving operability two-sided — POSITIVE required-capability checks prove the owner actually holds USAGE on public and SELECT/INSERT on each exact target table (readiness can never be true for a safe-but-inoperable boundary), and because the six target tables have RLS enabled with SELECT-only policies and no INSERT policy, the migration adds owner-scoped RLS policies (SELECT on all six, INSERT on the four import tables,`TO forever_import_execution_owner`only,`USING/WITH CHECK (true)`, no UPDATE/DELETE, no broad role — safe because the NOLOGIN owner is reachable only through the request-validating SECURITY DEFINER wrapper) with a static RLS audit that grants no BYPASSRLS and transfers no target-table ownership; and (review 11) making the RLS policy state deterministic and proving its EXACT runtime definition — the migration normalizes each of the ten dedicated per-table policies (six `forever_import_owner_select`policies and four`forever_import_owner_insert` policies, one per exact table) with`DROP POLICY IF EXISTS`+ exact`CREATE POLICY ... AS PERMISSIVE`(never`IF NOT EXISTS`, which would preserve a same-name policy with drifted unsafe/inoperable semantics; only the ten uniquely-named RC5.5D policies are ever dropped, no unrelated application policy), and the runtime audit proves each policy's exact name, exact table, exact command, `polpermissive`, EXACT role set by oid-array equality (`polroles = {owner}`— never "owner appears somewhere in polroles", so owner-plus-authenticated/anon/service_role/any-custom-role, PUBLIC, and empty role sets all fail), and exact constant-true`USING`/`WITH CHECK`expressions via`pg\*get_expr`under a tight normalization (only`true`/`(true)`; `false`, helper routines, session settings, subqueries, JWT claims, and current_user conditions all fail); PUBLIC applicability is handled through the `polroles`OID-0 semantics (PUBLIC is a pseudo-group invisible to a`pg_roles` join and applies to every role), and EVERY policy on the six target tables applicable to the owner — directly, via PUBLIC, or via any transitive membership (recursive closure, correct even if the required-empty closure ever changes) — is classified as exactly one of required_execution_policy / approved_preexisting_read_policy / unexpected_policy, with the six pre-existing PUBLIC website SELECT policies individually enumerated with their complete expected definitions (all permissive SELECT-only, so they can neither widen the owner's audited access nor block it) and ANY other applicable policy — a new harmless-looking SELECT policy, a definition-drifted known name, an applicable RESTRICTIVE policy (which would AND together with the permissive set and could silently deny required rows), an applicable UPDATE/DELETE/ALL policy, or a PUBLIC write policy — independently blocking readiness fail-closed) — table DML (SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER on tables/partitioned/views/matviews/foreign tables), sequence privileges, schema CREATE, schema USAGE, and executable SECURITY DEFINER functions are checked across every schema not on the explicit system allowlist (`pg_catalog`/`information_schema`/`pg_toast`/`pg_temp\_\_`/`pg\*toast_temp\*\*`), so Supabase/extension schemas (`auth`/`storage`/`extensions`/`graphql`/`vault`) are audited, a deceptive name like `pg_catalog_evil`is not excluded, and the approved wrapper is the sole allowed privileged callable surface. The migration deliberately does NOT globally`REVOKE USAGE ON SCHEMA public FROM PUBLIC` (a separate Owner decision). The audit MUST run against the real target after migration application and before approval issuance, blocking readiness on any unexpected effective privilege, ownership, another executor-reachable SECURITY DEFINER function, a role membership/SET ROLE path, or CREATE right; a PUBLIC-derived public-schema USAGE (or database TEMP) is classified explicitly as blocking pending a separate Owner-approved target-ACL reconciliation (option B), never silently assumed absent. The live transport is a single-purpose DIRECT-PostgreSQL transport (`ApprovedImportDatabaseTransport.executeApprovedImport(request)`), not PostgREST: it runs exactly one fixed parameterized statement `SELECT forever_execution.forever_execute_approved_import($1::jsonb)`with the request as the only bound parameter, and no caller supplies a schema, table, function name, or SQL and no generic database client escapes. Everything stays disabled: the adapter requires an injected capability that is false by default AND an explicitly injected transport (the default factory fails closed),`LIVE_SERVER_EXECUTION_ENABLED`and`executeEnabled` remain false, the ordinary CLI cannot reach the server path at all, the migration is not applied, no Supabase connection is made, and real database writes remain zero. Hermetic coverage only (fakes and static SQL analysis): migration privilege/security validation, durable CAS and concurrency (exactly one winner), database-time expiry, client-restart and repeat/recovery policy (same request, same approval, different approval for an imported plan, registry unavailable, stale fingerprint, changed plan hash, changed target state), the 405-operation Coralina-shaped request committing end-to-end, rollback at early/middle/late/verification/commit stages with zero partial durable state, and secret/URL/SQL/provider-text leakage prevention. Production remains blocked, staging unconfigured, Factory autonomy A0, and the first real Coralina import (approval issuance, migration application, capability enablement) remains a separate Owner checkpoint.

## Notes

- FDB-001 added the Forever Core Database foundation as additive Supabase migrations.
- FDB-002 established Modeva extraction, unit import prerequisites, import migration, and validation.
- FDB-003 created the reusable Import Engine and proved Modeva remains idempotent at 7 buildings, 289 units, and 289 price history rows with no duplicates.
- RC3-001 hardened the Import Engine boundary around manifest validation, extracted dataset loading, import planning, relationship validation, dry-run safety, rollback preparation, and database insertion separation.
- RC3-002 loads Coralina extracted datasets, creates a canonical internal Project object only after readiness passes, returns a blocked summary when readiness fails, and does not import units, buildings, media, relationships, Intelligence, or Passport data.
- RC3-003 derives canonical Building objects from source-backed price-list building facts, appends Building operations after Project in dry-run plans, keeps Units and Prices at zero, and leaves Coralina blocked until readiness blockers are resolved.
- RC3-004 loads `extracted/unit-plans.json`, creates canonical Unit operations after Buildings, keeps Prices at zero, blocks execute mode, plans Modeva as Project + 7 Buildings + 289 Units, and leaves Coralina blocked until readiness blockers are resolved.
- RC3-005 derives canonical Price History operations from source-backed price-list rows after Units, validates unit references, numeric prices, price dates, currency presence or explicit null warnings, and duplicate source keys, blocks execute mode, plans Modeva as Project + 7 Buildings + 289 Units + 289 Price History rows, and leaves Coralina blocked until readiness blockers are resolved.
- FDM-001 created the Modeva source material folder structure.
- FDM-002 created the Forever project import manifest standard.
